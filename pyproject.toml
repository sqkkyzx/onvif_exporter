[project]
name = "onvif-exporter"
version = "0.2.7"
description = "ONVIF/CV Exporter for Prometheus"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "uvicorn",
    "prometheus-client",
    "onvif-zeep",
    "opencv-python-headless",
    "ffmpeg-python"
]
