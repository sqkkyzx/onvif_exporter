[project]
name = "onvif-exporter"
version = "0.1.2"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "uvicorn",
    "prometheus-client",
    "onvif-zeep",
    "opencv-python-headless",
    "ffmpeg-python"
]