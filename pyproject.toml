[project]
name = "onvif-exporter"
version = "0.1.5"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "uvicorn",
    "prometheus-client",
    "onvif-zeep",
    "opencv-python-headless",
    "ffmpeg-python"
]