[project]
name = "onvif-exporter"
version = "1.0.0b0"
description = "ONVIF/CV Exporter for Prometheus"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "uvicorn",
    "prometheus-client",
    "onvif-zeep",
    "opencv-python-headless",
    "ffmpeg-python"
]
